import React from 'react'; // Optional in newer Next.js, but ensures it's treated as a module

export default function EnquiryPage() {
    return (
        <div>
            <h1>Enquiry Admin Page</h1>
        </div>
    );
}